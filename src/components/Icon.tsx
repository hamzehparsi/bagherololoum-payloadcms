import Logo from '@/components/Logo'

export default function Icon() {
  return (
    <Logo
      imageStyle={{
        width: '100%',
        height: '100%',
        maxWidth: 'none',
      }}
    />
  )
}
